services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gigit
      POSTGRES_PASSWORD: gigit
      POSTGRES_DB: gigit
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
